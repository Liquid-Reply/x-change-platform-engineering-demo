apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Minikube overlay
# IMPORTANT: This file is generated/updated by the installer
# The REPO_URL_PLACEHOLDER will be replaced with your actual repository URL

resources:
  - ../../base

# Repository URL for this environment
# Placeholder will be replaced by installer with actual repo URL
patches:
  # Apps with spec.source (single source) - patch repoURL and targetRevision
  - target:
      kind: Application
      name: argoconfig
    patch: |-
      - op: replace
        path: /spec/source/repoURL
        value: "REPO_URL_PLACEHOLDER"
      - op: replace
        path: /spec/source/targetRevision
        value: "TARGET_REVISION_PLACEHOLDER"
  - target:
      kind: Application
      name: argo-rollouts
    patch: |-
      - op: replace
        path: /spec/source/repoURL
        value: "REPO_URL_PLACEHOLDER"
      - op: replace
        path: /spec/source/targetRevision
        value: "TARGET_REVISION_PLACEHOLDER"
  - target:
      kind: Application
      name: backstage
    patch: |-
      - op: replace
        path: /spec/source/repoURL
        value: "REPO_URL_PLACEHOLDER"
      - op: replace
        path: /spec/source/targetRevision
        value: "TARGET_REVISION_PLACEHOLDER"
  - target:
      kind: Application
      name: dynatrace
    patch: |-
      - op: replace
        path: /spec/source/repoURL
        value: "REPO_URL_PLACEHOLDER"
      - op: replace
        path: /spec/source/targetRevision
        value: "TARGET_REVISION_PLACEHOLDER"
  - target:
      kind: Application
      name: ingress-nginx
    patch: |-
      - op: replace
        path: /spec/source/repoURL
        value: "REPO_URL_PLACEHOLDER"
      - op: replace
        path: /spec/source/targetRevision
        value: "TARGET_REVISION_PLACEHOLDER"
  - target:
      kind: Application
      name: namespaces
    patch: |-
      - op: replace
        path: /spec/source/repoURL
        value: "REPO_URL_PLACEHOLDER"
      - op: replace
        path: /spec/source/targetRevision
        value: "TARGET_REVISION_PLACEHOLDER"

  # Apps with spec.sources (multi-source) - patch first element
  - target:
      kind: Application
      name: kubeaudit
    patch: |-
      - op: replace
        path: /spec/sources/0/repoURL
        value: "REPO_URL_PLACEHOLDER"
      - op: replace
        path: /spec/sources/0/targetRevision
        value: "TARGET_REVISION_PLACEHOLDER"
  - target:
      kind: Application
      name: opentelemetry
    patch: |-
      - op: replace
        path: /spec/sources/0/repoURL
        value: "REPO_URL_PLACEHOLDER"
      - op: replace
        path: /spec/sources/0/targetRevision
        value: "TARGET_REVISION_PLACEHOLDER"

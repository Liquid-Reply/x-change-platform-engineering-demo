apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Minikube environment overlay
# - Base platform manifests only
# - No Dynatrace (add ../../platform/dynatrace if credentials available)
# - No Keptn (add ../../platform/keptn if needed)

resources:
  - ../../base

# Uncomment to enable optional features:
# - ../../platform/dynatrace
# - ../../platform/keptn

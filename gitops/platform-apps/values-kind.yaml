# Kind cluster environment overrides
#
# This file contains environment-specific values for Kind deployments.
# Only values that differ from values.yaml need to be specified here.

global:
  # Use the feature branch for Kind development
  targetRevision: "gh_opus_kostumize2helm"
